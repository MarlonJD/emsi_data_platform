# dbt Macros

Use this folder for EMSI-owned dbt macros only. Data Vault helper macros should
come from the selected `ScalefreeCOM/datavault4dbt==1.18.3` package unless there
is a documented reason to override them.
